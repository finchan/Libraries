DELIMITER $$
USE `jdbc` $$
CREATE PROCEDURE `retreive_different_results` ()
  BEGIN
    SELECT DISTINCT NAME FROM USERS WHERE ID=1;
    SELECT DISTINCT EMAIL FROM USERS;
    SELECT COUNT(ID) AS USER_COUNT FROM USERS;
  END$$
DELIMITTER ;
end $$